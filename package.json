{
  "name": "white-label-server-reseller-bot",
  "version": "1.0.0",
  "private": true,
  "description": "White-label Telegram bot for reselling cloud servers through an upstream API",
  "main": "src/bot.js",
  "scripts": {
    "start": "node src/bot.js",
    "check": "node --check src/config.js && node --check src/core-api.js && node --check src/db.js && node --check src/bot.js",
    "test": "npm run check"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "dotenv": "^16.6.1",
    "node-telegram-bot-api": "^0.66.0",
    "sqlite3": "^5.1.7"
  }
}
